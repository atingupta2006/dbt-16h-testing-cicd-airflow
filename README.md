# DBT 16 Hours

Live demos + post-training practice repo for the TOC in `course-content.txt`.

| Path | Shared with students? |
|------|------------------------|
| `GH/` | Yes — guides |
| `dbt_project/` | Yes |
| `airflow/` | Yes |
| `.github/workflows/` | Yes |
| `data/raw/` | Yes (CSV files; trainer loads RAW — not a taught topic) |
| `scripts/` | Yes (helpers; warehouse bootstrap is env prep only) |
| `internals/` | **No** — gitignored (trainer only) |

**Students:** start at [`GH/README.md`](GH/README.md).  
**Trainers:** use local `internals/` (not in the shared git tree).
